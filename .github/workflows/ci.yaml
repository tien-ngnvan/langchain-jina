name: CI Workflow

on:
  push:
    branches:
      - workflow
  pull_request:
    branches:
      - origin  # You can adjust this if your default branch is different (e.g., `master`)

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'  # Python version you want to use, could be a variable

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry  # Install Poetry

  install:
    runs-on: ubuntu-latest
    needs: setup  # Ensure `setup` job completes first
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install poetry
          poetry install  # Installs dependencies as per pyproject.toml

  lint:
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install poetry
          poetry install  # Install the necessary dependencies

      - name: Run lint (Ruff)
        run: |
          source venv/bin/activate
          poetry run ruff lint

  test:
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install poetry
          poetry install

      - name: Run tests with pytest
        run: |
          source venv/bin/activate
          poetry run pytest

  coverage:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install poetry
          poetry install

      - name: Run coverage
        run: |
          source venv/bin/activate
          poetry run coverage run -m pytest
          poetry run coverage report

  mypy:
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install poetry
          poetry install

      - name: Run mypy
        run: |
          source venv/bin/activate
          poetry run mypy langchain_jina  # Replace with your actual package name if different
